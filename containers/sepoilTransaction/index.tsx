import ProductCard from "@/components/ProductCard";
import React from "react";

const SepoilTransactionComponent = ({ product }) => {
  return (
    <div>
      <ProductCard product={product} />
    </div>
  );
};

export default SepoilTransactionComponent;
